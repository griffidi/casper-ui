{
  "name": "workspace",
  "description": "Monorepo with React demo, component library and a GraphQL API",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=22.16.0"
  },
  "type": "module",
  "scripts": {
    "clear": "rimraf -g pnpm-lock.yaml **/node_modules",
    "lint": "pnpx biome lint"
  },
  "devDependencies": {
    "@biomejs/biome": "2.0.6",
    "@types/node": "latest",
    "esbuild": "0.25.5",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
